{
  "name": "nurchain",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -n server,client -c auto \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "build": "npm --prefix server run build && npm --prefix client run build",
    "start": "concurrently -n server,client -c auto \"npm --prefix server start\" \"npm --prefix client start\"",
    "bootstrap": "npm --prefix server install && npm --prefix client install"
  },
  "dependencies": {
    "concurrently": "^9.0.0",
    "react-icons": "^5.5.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
